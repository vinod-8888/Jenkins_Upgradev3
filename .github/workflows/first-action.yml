name: First Workflow
on: workflow_dispatch
jobs:
  first_job: 
       runs_on: ubuntu-latest
       steps:
          - name: Print Greeting
            run: echo "hello world"
          - name: print goodbye
            run: echo "good bye"
          
